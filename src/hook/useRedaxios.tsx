import axios from 'redaxios';
import {createContext, ReactNode} from "react";

export const RedaxiosContext = createContext(null);

export const RedaxiosProvider = ({children}: {children: ReactNode}) => {

}